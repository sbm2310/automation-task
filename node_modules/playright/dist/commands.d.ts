import { Element } from './element';
/**
 * TODO: consider moving impl. from Located to here, and then reuse in Located
 * currently we reuse here the impl. from Located
 */
export declare namespace perform {
    const press: (text: string, { delay, noWaitAfter }?: {
        delay?: number;
        noWaitAfter?: boolean;
    }) => (located: Element) => Promise<Element>;
    const type: (text: string, { delay, noWaitAfter }?: {
        delay?: number;
        noWaitAfter?: boolean;
    }) => (located: Element) => Promise<Element>;
    const click: (options?: {}) => (located: Element) => Promise<Element>;
}
//# sourceMappingURL=commands.d.ts.map